<TabViewItem title="Chats" loaded="loaded" navigatingTo="navigatingTo">
  <TabViewItem.view>
  <GridLayout>
<ListView items="{{chats}}">
<ListView.itemTemplate>
<GridLayout>
 <!--<ion-item
              ng-repeat="chat in chats.data | orderBy:'-lastMessage.timestamp'"
              class="item-chat item-remove-animate item-avatar item-icon-right"
              type="item-text-wrap">
        <img ng-src="{{ chat.picture }}">
        <h2>{{ chat.name }}</h2>
        <p>{{ chat.lastMessage.text }}</p>
        <span class="last-message-timestamp">{{ chat.lastMessage.timestamp }}</span>
        <i class="icon ion-chevron-right icon-accessory"></i>-->
        
        <Image src="{{picture}}"/>
        <Label text="{{name}}"/>
        <Label text="{{lastMessage.text}}"/>
        <Label text="{{lastMessage.timestamp}}"/>
</GridLayout>
</ListView.itemTemplate>
</ListView>
</GridLayout>
  </TabViewItem.view>
</TabViewItem>